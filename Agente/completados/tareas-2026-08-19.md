# Tareas completadas — 2026-08-19

## 128A-1 — Frontend v2: bloque de landing archivado como referencia

- **Que:** A petición del usuario se archivó el bloque de diseño actual de `frontend-v2`
  (secciones Nuestra misión, Comunidad y colaboración, y el llamado a donación) para
  liberar la página y empezar otra cosa. El bloque quedó fuera de la ruta de render,
  en `src/archivado/landing-v1/`, como referencia consultable sin estorbar.
- **Archivos:**
  - Movidos a `frontend-v2/src/archivado/landing-v1/`:
    - `mision/MisionAgape.{tsx,css}`
    - `alianzas/TarjetasAlianzas.{tsx,css}`
    - `TarjetaAgape.{tsx,css}` (componente compartido por las secciones)
  - `frontend-v2/src/App.tsx`: se quitaron los imports y el render de misión, alianzas y
    llamado a donación; queda barra de anuncio, navegación, hero y pie, con un comentario
    que apunta al archivo de referencia.
  - Imports corregidos en los archivos archivados para que el código siga compilando
    (`TarjetaAgape` → `../TarjetaAgape` desde las subcarpetas; `BotonEnlace` →
    `../../components/ui/BotonEnlace`).
  - `frontend-v2/src/features/mision/` y `frontend-v2/src/features/alianzas/` quedaron
    vacías y se eliminaron. `TarjetaAgape` ya no está en `components/ui/`.
- **Evidencia:** `npm --prefix frontend-v2 run type-check` → OK (sin errores). La página
  `http://localhost:5176` recargó y muestra solo barra de anuncio, navegación, hero y pie;
  las secciones archivadas ya no se renderizan. El hero mantiene el enlace a `#mision`
  (ancla que hoy no existe) pendiente de la próxima sesión de diseño.
- **Decisiones / gotchas:**
  - El archivado es dentro de `src/` (`src/archivado/landing-v1/`) para conservar la
    compilación y la referencia viva; no se borró el bloque.
  - La próxima sesión debe retomar el diseño desde `src/App.tsx` y decidir qué pasa con
    el enlace `#mision` del hero y de la navegación.
- **Sentinel:** no aplica.
- **GLORY:** no aplica (contenido específico del cliente; no se movió a `glory-rs/`).
- **Pendientes:** decidir con el usuario qué se construye ahora en `frontend-v2`; si se
  retoma `landing-v1`, restaurar desde `src/archivado/` o rehacer el bloque.

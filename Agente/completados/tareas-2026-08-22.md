# Tareas completadas — 2026-08-22

## 218A-1 — Frontend v2: página de donación `/donar` + panel administrativo con login

- **Qué:** A petición del cliente se construyó la página pública de donación `/donar`
  (inspirada en dona.yummyrides.com pero con el estilo v2 de la landing: píldora,
  Playfair/Rubik, tarjetas, tokens) y se migró el panel administrativo a frontend-v2
  con login JWT y gestión de blog, recibos, métodos de pago y transparencia,
  reutilizando los endpoints `/api/admin/*` del backend (sin adaptadores PayPal/Stripe
  reales ni escrituras en proveedores externos).
- **Archivos nuevos (`frontend-v2/src/features/donar/`):**
  - `Donar.tsx` — formulario: montos sugeridos `[10,20,50,100,200]` + "otro", datos del
    donante (nombre opcional + correo), métodos desde `/api/payment-methods`
    (instrucciones del manual visibles), botón que genera `mailto:
    hola@elproyectoagape.org` (sin backend de pagos aún) y estado de confirmación con
    enlace a `/`.
  - `Donar.css` — estilos solo con tokens (`variables.css`): `.etiquetaDonar`
    (píldora), `.donarHero` centrado, `.recaudadoDonar` chip amarillo con punto vivo,
    `.donarFormulario` (max-width 720px), `.montoDonar`/`.metodoDonar` seleccionables
    rojos, `.botonDonar` rojo radio 40.5px, `.confirmacionDonar`.
  - `donarApi.ts` — tipos `MetodoPagoPublico` (`public_config.instructions`) y
    `ResumenTransparencia`; `cargarMetodosPago`, `cargarResumenTransparencia`,
    `formatearMonto` (carga con `AbortController`).
- **Archivos nuevos (`frontend-v2/src/features/admin/`):**
  - `PuertaAdmin.tsx` — render-prop `(perfil, token) => ReactNode`; token en
    `sessionStorage` (`agape_admin_token`); valida contra `/api/admin/me`; formulario
    login correo/clave.
  - `apiAdmin.ts` — `peticion<T>` genérico con Bearer; `iniciarSesion`, `obtenerPerfil`,
    `adminGet`, `adminEscribir`; tipos `ArticuloAdmin` y `DatosArticulo`; `listarArticulos`,
    `crearArticulo`, `guardarArticulo`, `cambiarEstadoArticulo`; `publicarEntrada`
    corregida → `PUT /api/admin/transparency/entries/:id/status` `{status:'published'}`.
  - `PanelAdmin.tsx` — 4 pestañas (Resumen, Recibos de pago, Métodos de pago, Blog);
    cabecera con correo + rol + botón Salir; enlace "Ver sitio público".
  - `PanelAdmin.css` — `.panelFormulario`, `.panelFormularioFila` (2 col ≥720px),
    inputs/textareas estilizados, `.panelOpcional`.
  - `VistaBlog.tsx` — guard `puede(perfil.role,'ManageContent')`; form (título, slug,
    resumen, contenido con doble salto = párrafos, portada opcional); tabla con link
    público a `/blog/:slug` si published; acciones Editar/Publicar/Archivar; gotcha
    `body.replace(/\r\n/g,'\n')` por el seed con `\r\n` literal.
  - `VistaResumen.tsx` — ruta `/transparency/entries`; tabla últimos 8 movimientos con
    chips de estado.
  - `VistaRecibos.tsx` — tipo sin `donor_email`; revisión `PUT
    /payment-receipts/:id/review`; `puede(role,'ReviewLedger')`.
  - `VistaMetodos.tsx` — `puede('ManagePaymentMethods')`; Habilitar deshabilitado para
    automáticos `setup_required`.
  - `permisos.ts` — espejo de `domain/permissions.rs` (ReadPanel, WriteLedger,
    ReviewLedger, ManageContent, ManagePaymentMethods, ReadAudit).
- **Archivos modificados:**
  - `frontend-v2/src/App.tsx` — rutas `/donar` y `/admin` registradas.
  - `frontend-v2/src/components/ui/BotonEnlace.tsx` — prop `interno` → `<Link>` de
    react-router si true.
  - `frontend-v2/src/components/layout/NavegacionPrincipal.tsx`, `PiePagina.tsx`,
    `frontend-v2/src/features/inicio/HeroInicio.tsx` — botones "Quiero ayudar" /
    "Sé parte del cambio" cableados a `/donar` (interno).
- **Evidencia:**
  - Backend canónico: `npm run dev:back` (wrapper `scripts/run-with-db.mjs`, BD de
    rama `ong_agame_backend_ong_agape`, target `C:\tmp\glory-target\ong_agame_backend_ong_agape`)
    en `http://localhost:3000`; frontend-v2 `npm run dev` en `http://localhost:5176`
    con proxy default a `localhost:3000` (verificado: `GET /api/health` 200 a través
    del proxy; **no** usar `BACKEND_PROXY=3010`, el backend real escucha en 3000).
  - `npm run type-check` (frontend-v2) → limpio.
  - Navegador `http://localhost:5176/donar`: hero + chip "$0 recaudados" + montos +
    datos; con método "Pago móvil" habilitado desde el panel, aparece la tarjeta
    "Pago móvil · Transferencia · verificación manual" y el botón actualiza el mailto
    ("vía Pago móvil").
  - Navegador `http://localhost:5176/admin`: login con cuenta owner de prueba
    (registro vía `POST /api/auth/register` con correo en `ADMIN_EMAILS` del `.env`,
    luego rol owner verificado en `/api/admin/me`) → panel con 4 secciones; pestaña
    Blog lista los 3 posts seed; crear borrador → "Artículo creado como borrador" →
    Publicar → "Artículo publicado" → enlace público `/blog/prueba-panel-v2`;
    pestaña Métodos habilita/deshabilita manuales (automáticos bloqueados en
    `setup_required`).
  - Datos de prueba limpiados tras validar: artículo `prueba-panel-v2` borrado de la
    BD (`DELETE 1`) y Pago móvil devuelto a `disabled`; BD final: 3 artículos admin,
    3 públicos, 0 métodos enabled. Cuenta owner local `owner@elproyectoagape.org`
    queda creada como utilidad local; `ADMIN_EMAILS` del `.env` restaurado a vacío.
- **Decisiones / gotchas:**
  - El proxy de Vite apunta a `localhost:3000` por defecto; nunca fijar
    `BACKEND_PROXY` a un puerto distinto salvo cambio explícito.
  - El backend NO se lanza con `cargo run` a pelo junto al orchestrator: usar
    `npm run dev:back` (wrapper con BD/target de rama); el orchestrator `npm run dev`
    raíz falla con `AddrInUse` si el backend ya está corriendo.
  - `publicarEntrada` del front usa `PUT .../entries/:id/status` (no POST).
  - `VistaBlog` normaliza `\r\n` del body porque el seed almacena `\r\n` literal.
  - `/api/payment-methods` devuelve `[]` hasta habilitar un método manual desde el
    panel (rol owner); los automáticos requieren credenciales reales.
- **Pendientes reales:** conectar pagos reales (requiere confirmación explícita del
  cliente), CRUD de campañas, pruebas automatizadas de autorización/responsive QA
  (línea 118A-1), decidir cuándo frontend-v2 reemplaza a frontend/ (128A-1).
- **Gate:** pendiente de `sentinel check` al cerrar el bloque de cambios completo
  (landing v2 + donar + panel siguen sin commitear, a la espera de aprobación del
  usuario; no se hace push/commit sin aprobación).

## 226A-1 — Mejoras de panel, aliados, campañas y página de donar (2026-08-22)

- **Qué:** segundo bloque de mejoras pedidas por el cliente sobre frontend-v2:
  filtros en tablas del panel, botón de publicar en blog, cabecera de sección con
  h2 a la izquierda y botón a la derecha, carrusel infinito de aliados, CRUD de
  aliados, CRUD de campañas (meta única activa), donaciones en vivo con nombre
  obligatorio y selector de método de pago estilo dona.yummyrides.com.
- **Archivos nuevos:**
  - `frontend-v2/src/features/inicio/Aliados.tsx` + `Aliados.css` +
    `aliadosApi.ts` — sección de aliados consumida desde `/api/allies`, carrusel
    infinito (dos grupos `.grupoPista` con `padding-right` = gap y
    `translateX(-50%)`), `mask-image` en los bordes, `prefers-reduced-motion`
    desactiva la animación.
  - `frontend-v2/src/features/admin/VistaAliados.tsx` — CRUD completo de aliados
    (nombre, URL de logo, orden, visible/activo) con guard `ManageContent`.
  - `frontend-v2/src/features/admin/VistaCampanas.tsx` — CRUD de campañas
    (nombre/slug/moneda/meta/fechas/descripción) con estados draft/active/
    completed/archived y acciones Activar/Completar; la campaña `active` define
    la meta de la página de donar.
  - `frontend-v2/src/features/inicio/Aliados.css` — estilos del carrusel.
  - `migrations/20260826000000_allies.up.sql` / `.down.sql` — tabla `allies`
    (id, nombre, logo_url, display_order, active, created_by/updated_by,
    created_at/updated_at) con índice parcial público.
  - `src/models/allie.rs`, `src/repositories/allie.rs`, `src/handlers/allie.rs` —
    modelo, repositorio y handlers CRUD de aliados (público + admin).
  - `src/models/donation.rs` — tipo `LiveDonation`.
- **Archivos modificados:**
  - `frontend-v2/src/features/admin/VistaResumen.tsx` — filtros Tipo (todos/
    ingresos/gastos) y Estado (todos + estados) sobre los movimientos.
  - `frontend-v2/src/features/admin/VistaRecibos.tsx` — filtros Estado y Método.
  - `frontend-v2/src/features/admin/VistaBlog.tsx` — filtro Estado, cabecera
    `.panelCabeceraSeccion` (h2 izquierda, botón derecha) y botón "Guardar y
    publicar" (crea o guarda y luego publica).
  - `frontend-v2/src/features/admin/PanelAdmin.tsx` — pestañas Aliados y Campañas.
  - `frontend-v2/src/features/admin/PanelAdmin.css` — `.panelCabeceraSeccion`,
    `.panelFiltros`, `.panelLogoAliado`, `.panelDescripcionSeccion`,
    `.panelCheckbox`.
  - `frontend-v2/src/features/admin/apiAdmin.ts` — tipos y funciones CRUD de
    aliados y campañas.
  - `frontend-v2/src/features/donar/Donar.tsx` + `Donar.css` + `donarApi.ts` —
    meta de recaudación (título + monto desde la campaña activa), donaciones en
    vivo (avatar con inicial + nombre + "donó US$ X"), nombre obligatorio, tarjetas
    de método con subtítulo (`subtituloMetodo`), eliminado "recaudados hasta hoy".
  - `src/handlers/transparency.rs`, `src/repositories/payment.rs` — endpoint
    público `GET /api/donations/live` (últimas 12 donaciones aprobadas con
    `donor_name`).
  - `src/handlers/mod.rs`, `src/models/mod.rs`, `src/repositories/mod.rs` —
    registro de módulos, rutas y schemas OpenAPI.
- **Evidencia (navegador, 2026-08-22):**
  - `npm run type-check` (frontend-v2) → limpio.
  - `npm run check:back` (check + clippy, rama `ong_agame_backend_ong_agape`) →
    pasa con 0 warnings.
  - `/admin` login owner: filtros Resumen (Tipo=Ingresos → 6 filas de ingreso),
    filtros Recibos (Estado=Aprobado → 2 filas), filtro Blog (publicado → 4
    filas), pestaña Aliados (2 aliados + modal alta/edición), pestaña Campañas
    (campaña activa "Meta de alimentos para familias" US$ 500,000 con Editar/
    Completar).
  - Blog: botón "Guardar y publicar" crea artículo y lo publica (status "Artículo
    publicado"); archivado posterior lo retira del público (`/#blog` muestra solo
    los 3 posts originales).
  - `/donar`: meta "$128 de $5,000" con barra "3% recaudado", donaciones en vivo
    (María Fernández $25, Ana Rodríguez VES 50,000.00 con avatares), tarjetas de
    método "Pago móvil · transferencia verificada manualmente" y "Transferencia
    bancaria · verificación manual", campo nombre obligatorio.
  - Portada: carrusel de aliados con 2 grupos de pista y animación `desplazarPista`
    26s linear infinite, `mask-image` en bordes, sin hueco blanco.
- **Decisiones / gotchas:**
  - La campaña en estado `active` es la única que define la meta de `/donar`
    (título = nombre, monto = goal). Decisión tomada por defecto al no estar el
    usuario disponible; se puede ampliar a meta global separada si se pide.
  - El carrusel infinito usa dos copias del grupo de logos y anima
    `translateX(-50%)`; el `padding-right` del grupo debe ser igual al `gap` para
    que el bucle no tenga salto.
  - El nombre del donante pasa a ser obligatorio en `/donar` (aparece en las
    donaciones en vivo); no se guarda ningún dato de pago, solo se genera el
    `mailto` de confirmación.
  - Datos de prueba creados y dejados como demo: campaña activa "Meta de
    alimentos para familias", aliados "Fundación Sonrisas" y "Banco Solidario",
    métodos Pago móvil y Transferencia habilitados con instrucciones. El usuario
    puede borrarlos desde el panel.
- **Pendientes reales:** conectar pagos reales (confirmación del cliente),
  reportes/exportaciones de campañas, pruebas automatizadas de autorización/
  responsive QA, decidir cuándo frontend-v2 reemplaza a frontend/.
- **Gate:** pendiente de `sentinel check` al cerrar el bloque completo (sigue sin
  commitear, a la espera de aprobación del usuario para commit del lote).

## 226A-2 — Fase C de `/donar`: carrusel infinito, comprobante en la donación, datos de pago configurables y simulación de métodos (2026-08-22)

- **Qué:** cuarta tanda de mejoras pedidas por el cliente sobre la página de donar
  de frontend-v2: (1) carrusel infinito de donaciones en vivo con solo nombres de
  pila, (2) `.metaDonar` con el mismo ancho que `.donarFormulario`, (3) el
  comprobante se adjunta en la misma donación en lugar de enviarse por correo,
  (4) datos de pago móvil/transferencia editables en la configuración del panel y
  visibles en un modal en `/donar`, (5) verificación de la donación con los demás
  tipos de pago y (6) mostrar todos los métodos de pago (en simulación).
- **Archivos nuevos (`frontend-v2/src/features/donar/`):**
  - `CarruselDonaciones.tsx` — carrusel infinito de donaciones en vivo reutilizando
    el patrón de Aliados (`.cintaDonaciones` > `.pistaDonaciones` > 2 `.grupoPista`
    con `aria-hidden` en la segunda, cada uno con repeticiones `.copiaPista`
    `display:contents`). `primerNombre(nombre)` = `nombre.split(' ')[0] || nombre`;
    mide copias con `useCallback`/`medirCopias` y recalcula al cambiar las
    donaciones o al redimensionar. Devuelve `null` si no hay donaciones.
  - `ModalDatosPago.tsx` — modal `.modalDatosPago` con la cabecera "Datos de pago ·
    {public_label}", `dl.datosPago` con `instructions` y filas `FilaDato`
    (`bank_name`, `account_holder`, `account_number`, `account_phone`,
    `account_document`); cierra con X, ESC y clic en el overlay; mensaje de
    respaldo si no hay datos.
- **Archivos nuevos (`frontend-v2/src/features/admin/`):**
  - `ModalEditarMetodo.tsx` — modal `.panelModal` para editar un método de pago:
    etiqueta pública, instrucciones, banco/titular, número de cuenta/teléfono,
    documento/orden, estado (`enabled`/`disabled`/`setup_required`). Guarda con
    `adminEscribir('/payment-methods/'+id, token, 'PUT', cuerpo)` enviando solo
    campos no vacíos + `status` + `display_order`; exporta tipos `MetodoPagoAdmin`
    y `DatosFormulario` y `iniciales(metodo)`.
- **Archivos modificados:**
  - `frontend-v2/src/features/donar/Donar.tsx` — estados `comprobante` (File),
    `modalPagoAbierto`, `enviando`, `errorEnvio`; `metodoOperativo` (método
    habilitado + manual); `enviar()` asíncrono con `enviarDonacion` (FormData con
    `payment_method_id`, `donor_name`, `donor_email?`, `amount_minor`, `currency`,
    `provider_reference?`, `proof?`); bloque en vivo con `<CarruselDonaciones>`;
    tarjetas de método con clase `.metodoDonar--simulado` cuando no es
    manual/habilitado y subtítulo "· Próximamente"; botón "Ver datos de pago";
    fieldset de comprobante con `<input type="file">` (imagen/PDF ≤5MB) y
    `.nombreComprobanteDonar`; texto de confirmación "El comprobante quedó
    adjunto a tu donación"; `errorEnvio` con `role="alert"`.
  - `frontend-v2/src/features/donar/Donar.css` — `.metaDonar` `width:min(100%,720px)`;
    estilos del carrusel (`.cintaDonaciones` con `mask-image`, `.pistaDonaciones`
    `animation: desplazarPista 32s linear infinite`, `.grupoPista`,
    `.copiaPista`, `@keyframes desplazarPista` al final del archivo, bloque
    `prefers-reduced-motion`); `.vivaDonar` `flex-shrink:0`; `.metodoDonar--simulado`
    `opacity:0.62`; `.datosPagoDonar`, `.botonDatosPago`, `.campoComprobanteDonar`,
    `.nombreComprobanteDonar`, `.errorEnvioDonar`; restaurados `.estadoDonar`,
    `.accionesDonar`, `.botonDonar` (con `:disabled`) y `.notaDonar`; estilos del
    modal `.modalDatosPago`/`.modalDatosPagoContenido`/`.modalDatosPagoCabecera`/
    `.modalDatosPagoCerrar`/`.datosPago`/`.instruccionesModalPago`/`.filaDatoPago`.
  - `frontend-v2/src/features/donar/donarApi.ts` — tipos `ConfigMetodoPublico`
    (instrucciones y 6 campos de cuenta, todos opcionales), `MetodoPagoPublico` con
    `status: 'enabled'|'disabled'|'setup_required'` y `public_config`; tipos
    `DatosDonacion` y `DonacionCreada`; `enviarDonacion(datos)` construye FormData
    y hace `POST /api/donations` **sin** Content-Type manual; `subtituloMetodo` para
    los 5 proveedores.
  - `frontend-v2/src/features/admin/VistaMetodos.tsx` — reescrita: importa
    `MetodoPagoAdmin` de ModalEditarMetodo; estado `editando`; botón Editar por
    método; `cambiarEstado` con `PUT {status}`; renderiza `<ModalEditarMetodo>`.
  - `frontend-v2/src/features/admin/PanelAdmin.css` — `.panelFormulario select`,
    `.panelFormulario select:focus-visible`, `.panelModalAcciones`,
    `.panelModalCancelar`, `.panelModalGuardar` (pill, `:disabled`).
- **Backend (verificado en sesión previa):**
  - `src/repositories/payment.rs` — `list_public_methods` devuelve TODOS los métodos
    (sin filtro `status='enabled'`) ordenados por `display_order`;
    `create_public_receipt` inserta en `payment_receipts` con `RETURNING
    id/status/provider_reference`.
  - `src/models/admin.rs` — `PublicPaymentMethod` con `status: String`;
    `NewPublicReceipt` (payment_method_id, provider_reference opcional, donor_name,
    donor_email opcional, amount_minor i64, currency, proof_url opcional);
    `PublicDonationReceipt {id, status, provider_reference}`.
  - `src/handlers/transparency.rs` — `list_public_payment_methods` devuelve todos
    con status; `create_donation` (multipart): valida método existente + manual +
    enabled (si no, `eliminar_comprobante` + 400), valida MIME (jpeg/png/webp/pdf)
    y ≤5MB, nombre de archivo UUID, devuelve 201 con
    `{id, status: pending_verification, provider_reference}`.
- **Evidencia (navegador, 2026-08-22):**
  - `npm run type-check` (frontend-v2) → limpio (0 errores).
  - `/donar`: carrusel infinito con animación `desplazarPista` 32s aplicada
    (transform -859.8 → -967.3px), `mask-image` aplicado, `.vivaDonar`
    `flex-shrink:0`; muestra Lucía, Pedro, Carlos, Sofía, María, Diego, Ana (solo
    nombres de pila).
  - Anchuras: `.metaDonar` = 720px = `.donarFormulario` = 720px.
  - Comprobante: input de archivo muestra el nombre, valida ≤5MB y se adjunta en el
    multipart.
  - Modal datos de pago: se abre con los datos de Banesco (Banco/Titular/Cuenta/
    Teléfono/Documento + instrucciones), cierra con X, ESC y overlay.
  - Donación: `POST /api/donations` → 201
    `{"id":"807ecc21-3023-45a5-88f7-a545c5a2c51e","status":"pending_verification",
    "provider_reference":null}`; confirmación "Registramos tu aporte de $20 vía
    Pago móvil. El comprobante quedó adjunto..."; recibo "Prueba Navegador" en
    panel Recibos con `proof_url /uploads/57ff4e90-925f-49ec-82db-8feb427b7907.png`.
  - Métodos simulados: PayPal/Stripe muestran "· Próximamente"; al seleccionarlos
    se deshabilita el botón Donar y aparece "Este método está en simulación...";
    Pago móvil (habilitado) habilita el botón.
  - Panel: "Métodos de pago" lista los 5 con Editar/Deshabilitar; el modal de
    edición carga todos los datos de Banesco; `PUT` → 200 con config actualizada;
    probado editar teléfono y REVERTIDO a `+58 412 000 0000`; el modal público
    refleja el estado revertido.
  - Recibos: "Prueba Navegador" (VES 20.00) y "Test Comprobante" (PRUEBA-REF-001,
    VES 125.00) en estado "Por verificar" con Aprobar/Rechazar.
  - Captura visual guardada en `C:\tmp\prueba-uploads\donar-phasec.png`.
- **Decisiones / gotchas:**
  - **Bug crítico corregido:** el `<input type="email" required>` bloqueaba el
    envío de forma silenciosa (validación nativa del navegador, el correo es
    opcional). Se quitó `required` con comentario explicando que el correo solo se
    usa para avisar al donante si el comprobante no se puede verificar.
  - `@keyframes desplazarPista` debe estar en el MISMO stylesheet que lo referencia
    (estaba solo en Aliados.css y `.pistaDonaciones` no lo veía).
  - Multipart: NO fijar Content-Type manual (el navegador pone el boundary);
    `adminEscribir` solo pone Content-Type cuando hay body, seguro para FormData.
  - El backend valida el método (existente + manual + enabled) y si no cumple
    elimina el comprobante subido y devuelve 400.
  - `public_config` jsonb se expone en el modal público solo con los campos
    poblados (los vacíos se omiten).
- **Pendientes reales:** conectar pagos reales (PayPal/Stripe requieren
  credenciales y confirmación del cliente; Zelle requiere cuenta propia), decidir
  qué hacer con los recibos de prueba en `pending_verification` ("Prueba
  Navegador" VES 20.00 y "Test Comprobante" VES 125.00, con sus archivos en
  `uploads/`) y con las capturas en `C:\tmp\prueba-uploads\`, pruebas
  automatizadas de autorización/responsive QA.
- **Gate:** pendiente de `sentinel check` al cerrar el bloque completo (sigue sin
  commitear, a la espera de aprobación del usuario para commit del lote).
